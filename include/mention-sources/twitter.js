const sources = require("../mention-sources.js");

exports.fetchMentions = (addMention, analyzeText, getCompanyByName) => {
    // get the mentions using the API

    // parse the text to determine sentiment
    // analyzeText(text, (stats) => {}

    // get the company id associated with the company
    // getCompanyByName(companyName, (id) => {});

    // add the mention using the callback above
    // addMention({
    //  "company_id" : company._id,
    //  "source" : source.TWITTER,
    //  "url" : response.url,
    //  "sentiment" : sentiment,
    //  "timestamp" : new Date(response.date)
    //})
}
